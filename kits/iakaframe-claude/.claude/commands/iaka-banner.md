---
description: Titre ASCII (FIGlet embarque, zero dep)
---

<!-- GENERE par cli/scripts/gen-iaka-commands.mjs depuis cli/src/lib/verbes.js (verbe `banner`).
     NE PAS EDITER A LA MAIN : la description derive de `resume`, regenerer via le script. -->

Affiche d'abord la ligne **`→ iakaframe banner $ARGUMENTS`** (la commande effectivement
exécutée), **PUIS** exécute **`iakaframe banner $ARGUMENTS`** et **restitue la sortie VERBATIM**
(aucune reformulation). Commande **lecture seule** : n'écrit rien, ne mute rien.

$ARGUMENTS
