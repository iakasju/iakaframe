---
description: Contrat d'un atome/assemblage : frontmatter + corps
---

<!-- GENERE par cli/scripts/gen-iaka-commands.mjs depuis cli/src/lib/verbes.js (verbe `show`).
     NE PAS EDITER A LA MAIN : la description derive de `resume`, regenerer via le script. -->

Affiche d'abord la ligne **`→ iakaframe show $ARGUMENTS`** (la commande effectivement
exécutée), **PUIS** exécute **`iakaframe show $ARGUMENTS`** et **restitue la sortie VERBATIM**
(aucune reformulation). Commande **lecture seule** : n'écrit rien, ne mute rien.

$ARGUMENTS
