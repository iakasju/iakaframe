---
description: Charge le canon (PROFIL+REGISTRE) a l'ouverture, scope-agnostique, pret a injecter en session (lecture seule)
---

<!-- GENERE par cli/scripts/gen-iaka-commands.mjs depuis cli/src/lib/verbes.js (verbe `open`).
     NE PAS EDITER A LA MAIN : la description derive de `resume`, regenerer via le script. -->

Affiche d'abord la ligne **`→ iakaframe open $ARGUMENTS`** (la commande effectivement
exécutée), **PUIS** exécute **`iakaframe open $ARGUMENTS`** et **restitue la sortie VERBATIM**
(aucune reformulation). Commande **lecture seule** : n'écrit rien, ne mute rien.

$ARGUMENTS
