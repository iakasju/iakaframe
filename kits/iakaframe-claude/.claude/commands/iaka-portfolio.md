---
description: Vue agregee du portefeuille (LECTURE SEULE) : def/version/arbre/commit/jalons
---

<!-- GENERE par cli/scripts/gen-iaka-commands.mjs depuis cli/src/lib/verbes.js (verbe `portfolio`).
     NE PAS EDITER A LA MAIN : la description derive de `resume`, regenerer via le script. -->

Affiche d'abord la ligne **`→ iakaframe portfolio $ARGUMENTS`** (la commande effectivement
exécutée), **PUIS** exécute **`iakaframe portfolio $ARGUMENTS`** et **restitue la sortie VERBATIM**
(aucune reformulation). Commande **lecture seule** : n'écrit rien, ne mute rien.

$ARGUMENTS
