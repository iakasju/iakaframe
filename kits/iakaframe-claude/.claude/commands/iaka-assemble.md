---
description: Compose un kit (methode+team[+binding]) - dry-run par defaut
---

<!-- GENERE par cli/scripts/gen-iaka-commands.mjs depuis cli/src/lib/verbes.js (verbe `assemble`).
     NE PAS EDITER A LA MAIN : la description derive de `resume`, regenerer via le script. -->

Affiche d'abord la ligne **`→ iakaframe assemble $ARGUMENTS`** (la commande effectivement
exécutée), **PUIS** exécute **`iakaframe assemble $ARGUMENTS`** et **restitue la sortie VERBATIM**
(aucune reformulation). Commande **lecture seule** : n'écrit rien, ne mute rien.

$ARGUMENTS
