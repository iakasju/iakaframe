---
id: self-hosted-first
label: Self-hosted / open-source d'abord
policy: "Pour tout choix de backend ou service, proposer la version locale ou auto-hébergée en option n°1 ; le cloud n'est qu'un fallback justifié."
trigger: "choix de backend, service ou dépendance"
---
# Self-hosted / open-source d'abord

Principe transverse iakaframe extrait de `methode-de-travail.md` et du CLAUDE.md global
(§ « Conventions permanentes ») — le narratif reste la référence (I5).

**Politique.** Pour tout choix de backend ou service, proposer la version locale ou auto-hébergée en option n°1 ; le cloud n'est qu'un fallback justifié.

**Déclencheur.** choix de backend, service ou dépendance.
