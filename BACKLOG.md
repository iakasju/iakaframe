# Backlog — iakaframe

Items de backlog du projet (tenus au fil de l'eau ; convertis en instruction cadrée avant tout dev).

## Ouverts

- [ ] **⏰ Remède kit du § 4.5 inopérant — À CORRIGER AVANT LE LOT DE RE-VENDORAGE** (gate vendor-check, 2026-07-20, **échéance datée, pas « un jour »**). Le CLI imprime aujourd'hui une commande de remédiation qui **ne peut pas** éteindre le rouge du kit. Mesuré par Legolas : golden dépouillé (10 l.) == fixture GUI → **true** ; `kits/iakaframe-claude.md` (16 l.) == fixture GUI → **false**. Or `iakaframe assemble iakaframe iakaframe-8 --write` écrit `kits/iakaframe-claude.md`, et la fixture doit égaler le **golden dépouillé** (A21) — **aucune relation d'égalité** entre les deux. Défaut du **cadrage** (§ 4.5 / A24), fidèlement implémenté par Gimli, qui a eu raison de ne pas le contourner (inventer un remède aurait créé une seconde source de vérité sur le format). Latent aujourd'hui — le kit est la seule dérivée propre. **Bloquant au lot de re-vendorage**, qui est précisément le moment où le kit dérivera. C'est « un rouge que personne ne sait éteindre » en miniature — ce que le § 4.5 existe pour empêcher. *Item Gandalf.*
- [ ] **§ 4.1 à amender — graver l'override autoritaire** (gate vendor-check, 2026-07-20). § 4.1 (« premier chemin existant gagne ») et A8 (« `IAKAFRAME_GUI_ROOT` inexistant ⇒ SKIP ») sont **contradictoires dans le cadrage** dès qu'un dépôt frère réel existe à côté. Gimli a tranché en code : **override autoritaire, jamais de repli silencieux**. Verdict Legolas : **arbitrage BON, niveau MAUVAIS** — le fond est juste (un repli ferait rendre un verdict confiant sur un dépôt non choisi, et rendrait A8/A9 intestables sur une machine où le frère est présent), mais trancher une contradiction de cadrage en code relève de Gandalf. Non bloquant : la décision est **opposable**, portée en clair par `docs/commandes.md`. *Item Gandalf.*
- [ ] **Câblage en dur des tools dans `parite-generateurs.test.ts` (dépôt iakaFrameGUI)** (gate vendor-check, 2026-07-20). Le test code en dur les tools de **gandalf, gimli et odin** — filet manuel couvrant **3 personas sur 8**, et rien ne signale les 5 autres. **Pire que déclaré** (trouvé par Legolas, pas par Gimli) : `toolsForPersona(binding, "odin")` y est attendu égal à `["Read","Grep","Glob","Bash"]`, soit la valeur **dérivée** de la fixture vendorée — la source porte `Task`. Le test n'est pas seulement partiel, il **épingle un de ses trois cas sur un état dérivé**. Explique l'échec de la première injection A5-b sur odin : comportement normal du filet, pas un défaut d'injection. *Hors périmètre du lot (§ 6 interdit de toucher aux tests GUI) — item du dépôt GUI.*
- [ ] **Test de non-mutation faible** (`cli/test/vendor-check.test.js`, tolérable relevé au gate) — il compare le porcelain GUI avant/après **à l'intérieur de lui-même** et n'assère jamais que la baseline était propre : un arbre déjà sale le ferait passer. Il verrouille un invariant réel (`vendor.js` ne contient aucune écriture, vérifié indépendamment par Legolas), mais faiblement. *Non bloquant.*
- [ ] **Tolérables de la phase 1 (registre T1-T5, matérialisé sur reco Legolas 2026-07-20)** — relevés au 8ᵉ gate du cadrage, classés **non disqualifiants** par le gate, à traiter en cours de série. Ils ne vivaient que dans les rapports de gate ; matérialisés ici pour survivre au changement de contexte. **T1** — ~~renvoi `§ 6.4` au lieu de `§ 6.5`~~ **TRAITÉ au lot Gimli (`c3d8ca4`)**, vérifié au gate : le renvoi pointe `§ 6.5`, et le § est barré + scellé (« 🔒 Caduc (levée LG-3) »). **T6** *(nouveau, relevé au gate du lot Gimli)* — le lot reproduit le motif de T3 : la charte a glissé de +2/+3 lignes et 3 pointeurs de sa propre instruction sont devenus faux — `persona-gimli-amelioration.md:206` cite `gimli.md:39` (réel `:42`), `:72` cite `gimli.md:16` qui porte désormais **l'inverse du propos cité**, `:41` cite une plage décalée. Citations fidèles au mot, pointeurs faux ; même classe que T2/T3, **non disqualifiant**. Correctif générique commun à T2/T3/T6 : **citer par nom de section, jamais par ligne**, dès qu'un lot antérieur touche le fichier. *Observation hors périmètre à traiter en phase 2 : `library/skills/iakaframe-jalon/SKILL.md` ne déclare pas de champ `layer` alors que `gestion-de-source` et `conteneurisation` portent `layer: capacity` — une capacité composée pointant une brique non typée est une asymétrie.* **T2** — `persona-gandalf-amelioration.md:113` et `:76` citent des plages fausses (`odin:66-67` → réel `73-74` ; `helm:70-73` → réel `77-80`) ; citations fidèles au mot, pointeurs faux. **T3** — `persona-nathalie-amelioration.md:66,151,174` cite `legolas.md:54-60` que L-1 fait glisser, Legolas passant désormais avant : **plages fausses à l'exécution** ; *à traiter au lot Nathalie, coût nul, correctif = citer par nom de section*. **T4** — `phase1-inventaire-bibliotheque.md:98` : `## 0.4` en niveau 2 quand 0.1-0.3 sont en niveau 3. **T5** — ~~atténuation non portée~~ **TRAITÉ au lot Loki (`1cb982f`), avec réserve** : la règle nommée `charte-defaut-conseil-pro` est réelle (la cellule porte un renvoi par nom, la valeur est écrite une seule fois — remplacement localisé effectif), mais **l'instruction n'a jamais été amendée pour T5** : `grep T5 persona-loki-amelioration.md` = 0, **aucun critère ne le couvre**, et LK-A4 passerait vert si la valeur revenait en cellule (son grep ne teste que « Cinabre / à confirmer »). Traité dans le code, absent du cadrage — vérifié par inspection directe au gate. *Deux observations de forme du canon, non bloquantes : le nom de règle résout vers **deux** définitions (`loki.md:59` et `iakaframe-naonedge/SKILL.md:38`, mêmes valeurs, proses différentes) et la déclaration de préséance « `loki.md` fait foi » vit **uniquement dans la copie** — un lecteur du canon ne sait pas qu'il tient le canon ; et `loki.md:57` porte un `*(idem)*` positionnel, la forme même que T5 visait à éliminer. Dédup = phase 2 (lot designs), mais l'assertion de canonicité côté `loki.md` aurait été gratuite.*

- [ ] **Garde `vendor-check` cross-repo (cause racine)** — aucune garde ne détecte une **dérive mutuellement cohérente** entre `iakaframe` et le miroir `iakaFrameGUI` : Legolas a démontré au gate v0.17.14 qu'un drift injecté à travers binding + golden + sha recalculés **ensemble** laisse **475/475 tests GUI verts**. Le lot v0.17.14 a corrigé l'instance (contrat fantôme d'Aragorn `Task` sans `Write`), pas la cause. Piste : un test qui lit les sources `iakaframe`, ou un `vendor-check` en pré-commit au niveau portefeuille. *Priorité haute — à cadrer (Gandalf).*
- [ ] **Dette des skills déployées (`~/.claude/skills/`)** — **22/22 en écart** : 14 skills périmées + 8 absentes, alors que les contrats d'agents sont à jour 8/8. Conséquence immédiate : `iakaframe-jalon` étant absente, le **geste de jalon introduit en v0.17.14 n'est pas actif en runtime**, et `iakaframe-aragorn` déployée parle encore de Slack. Contrairement aux personas, les skills n'ont **ni golden ni test de parité** — le chantier appelle un mécanisme de parité sur le modèle du générateur persona→contrat. *Acté par le décideur comme lot suivant (2026-07-19).*
- [ ] **Chantier personas — compléter le roster au-delà d'Aragorn** — l'audit à 7 dimensions n'a porté que sur Aragorn. Constats issus d'un relevé ciblé : (1) **Odin** porte la même incohérence que CH-1 — `guardrails: [… delegation]` + charte qui dispatche « via l'outil Agent » (`library/personas/odin.md:109`) alors que son binding n'a **pas `Task`** (`bindings/iakaframe-claude-default.md:7`) ; (2) **Gimli** a `skills: []` (`library/personas/gimli.md:8`) — seul agent du roster sans skill ; (3) le **geste jalon n'a pas irrigué le roster** — 0 mention chez Gimli/Loki/Nathalie/Odin, 1 seule chez Gandalf/Legolas/Helm, alors que Helm tient le gate prod et Legolas le verdict d'entrée en stage. *Ordre proposé : Odin → Gimli → jalon chez Helm/Legolas. À cadrer (Gandalf) avec le même sérieux que l'audit Aragorn.*
- [ ] **Réparer et porter le générateur de la vitrine HTML (Node)** — il n'existe **aucun** générateur `.md`→`.html` ; le seul script candidat, `iakaframe-build-methode-code.ps1`, ne remplit que le panneau « Code » de `methode-de-travail.html` (entre `<!--CODE_BLOCKS_START-->` L825 et `<!--CODE_BLOCKS_END-->` L2242) et il est **doublement mort** : il lit `agents/` et `skills/` à la racine (migrés depuis vers `library/personas/` et `library/skills/`), et `pwsh`/`powershell` sont **absents de la machine**. Sur les 18 « Slack » de `methode-de-travail.html`, **16 sont dans la zone générée** (copies figées d'anciennes skills/agents) — les réparer d'un geste suppose de réparer le générateur. Chantier : repointer les sources + **porter en Node** (lève au passage la dépendance `pwsh`, bloquante ailleurs). Attention : le repointage a aussi un morceau **dans la prose** — `methode-de-travail.html:802` cite `agents/` et `skills/` en chemins périmés. *Option (a) de l'arbitrage du 2026-07-19 ; l'option (b) — les 2 lignes de prose `:755` et `:802` — a été faite dans le lot mécanique.*
- [ ] **Perte du corps markdown au Save de la GUI (priorité — plus grave que le wrapping)** — `~/work/iakaFrameGUI/src/forge/ForgeShell.tsx:84-86` définit `methodBody` (idem `teamBody:82`, `kitBody:86`) comme un **boilerplate de deux lignes régénéré à chaque Save**. Si la GUI réécrivait `methods/iakaframe.md`, elle ne produirait pas un diff de wrapping : elle **écraserait tout le récit markdown du fichier** par « # Méthode iakaframe / … Forgé par iakaFrameGUI. ». Le wrapping n'était que le symptôme visible ; ceci est la vraie perte de fidélité. *Trouvé par Gimli hors instruction, 2026-07-19.*
- [ ] **Câbler le `layout` de préservation du wrapping (décision cross-repo)** — la capacité est livrée et testée côté GUI (`packages/core/src/frontmatter.ts`, `readListLayout` + `Field.wrap`, comportement par défaut **inchangé**), mais le câblage dans `ForgeShell.tsx:125` **n'a délibérément pas été fait** : `cli/src/lib/frontmatter.js:217-222` reflow **exactement pareil**, donc activer la préservation côté GUI seul **fabriquerait une divergence GUI≠CLI** — précisément la classe de défaut qu'aucune garde ne détecte. *À traiter AVEC le chantier `vendor-check` cross-repo, pas isolément.*
- [ ] **9 scories Slack orphelines (item de rattrapage — créé pour réparer un trou de rangement)** — le lot mécanique du 2026-07-19 a retiré l'item « scories Slack » des Ouverts en le remplaçant par l'item générateur, qui ne couvre QUE `methode-de-travail.html`. Résultat : 9 occurrences actives n'étaient plus couvertes par aucun lot, et `specs/instructions/parite-skills-generateur-deploiement.md:96` renvoyait à un item disparu. Décompte exact rétabli par Legolas (**13** occurrences restantes, pas 12 — `iakaframe-chapeau.html` en porte **deux**, `:294` et `:477`) : (a) **kit Ollama** `kits/iakaframe-ollama/AGENTS.md:14`, `MODELES.md:31,44,48` — « canal Slack » y sert de **critère de choix de modèle/harnais**, sa réécriture demande un arbitrage sur ce que doit couvrir le kit ; (b) `specs/equipe-agents.md:52,72,81` ; (c) **canon skill Odin** `library/skills/iakaframe-odin/SKILL.md:55,64` — même divergence skill↔persona que QW-1 avait traitée pour Aragorn (`library/personas/odin.md` est déjà migré). Les 4 occurrences des vitrines HTML (`iakaframe-chapeau.html:294,477`, `iakaframe-methode.html:438`, `doc/index.html:174`) restent rattachées à l'item générateur. **Point de jonction** : le critère B17 de l'instruction skills (`:293`, `grep -ri slack ~/.claude/skills/` = 0) butera sur le reliquat canon d'Odin — à traiter dans le lot skills ou avant lui.
- [ ] **`iakaframe jalon --help` plante** — `TypeError [ERR_PARSE_ARGS_UNKNOWN_OPTION]` avec stack trace nue, là où `consolidate` gère proprement `--help`. Pré-existant, hors périmètre du lot où il a été trouvé, mais c'est un **verbe de la méthode qui casse sur son propre `--help`** — et c'est le verbe du geste de jalon introduit en v0.17.14. *Trouvé par Legolas au gate du 2026-07-19.*
- [ ] **Fixture `method.iakaframe-wrapped.md` déclarée conforme à tort** — `~/work/iakaFrameGUI/packages/core/__tests__/methodMd.test.ts:47-49` affirme que la fixture est la « copie CONFORME » de `iakaframe/methods/iakaframe.md`. Le **frontmatter est byte-identique** (c'est lui qui porte le test, la validité est intacte) mais **le corps markdown est tronqué de 8 lignes**. Non bloquant, mais « copie déclarée conforme qui a silencieusement divergé de sa source » est **exactement** la classe de défaut du contrat fantôme de v0.17.14 — et cette fixture n'est couverte par aucune garde de vendorage. *À traiter avec le `vendor-check`.*
- [ ] **~82 pointeurs `chemin:ligne` périmés dans `specs/instructions/`** — les 4 instructions de phase 2 portent ~82 renvois de la forme `fichier.md:NN` (59 dans `audit-amelioration-roster-personas.md`, 12 dans `parite-skills-generateur-deploiement.md`, 11 dans `decision-rolekey-reconciliation.md`), dont une part vise les **7 personas modifiées en phase 1** et est donc **fausse**. Correctif générique déjà acté en phase 1 (tolérables T2/T3/T6) : **citer par nom de section, jamais par ligne**. Appliqué aux **canons**, **non rétro-appliqué aux instructions**. Traitement retenu au rafraîchissement du 2026-07-20 : les pointeurs antérieurs sont **scellés comme présumés faux** et doivent être **revérifiés avant usage** ; **aucune reprise en masse n'est demandée**. *Exception vérifiée : les pointeurs `library/personas/*.md:5` (ligne `roleKey` du frontmatter) restent **justes**.* *Dette de forme, non bloquante.*

## Fait

- [x] **Épuration des dettes mécaniques (3/3)** — (1) **scories Slack** : `README.md:173`, `library/skills/README.md:24` et `:168`, `iakaframe-skills.html:113`, + les 2 lignes de **prose** de `methode-de-travail.html` (`:755`, `:802`) après arbitrage option (c) du décideur ; distinction tenue — « n8n/Hermes » comme **outil de dispatch** intact, seul « Slack » comme **canal de communication** purgé ; les 16 occurrences de la zone générée laissées intactes à dessein (→ item générateur). (2) **`docs/commandes.md`** : compte exact rétabli — **29 verbes distincts + 1 alias (`use`→`switch`) = 30 `case`**, le « 26 » ne comptait que les lignes de table ; `consolidate`, `portfolio` et `observe` documentés (options vérifiées contre les `parseArgs` réels) ; clause de maintenance ajoutée (`:31-34`) — rafraîchir la date oblige à revérifier les compteurs dans le même geste. (3) **`serializeMethodMd`** (GUI) : capacité de préservation du wrapping livrée et testée (`readListLayout`, `Field.wrap`, 3ᵉ param optionnel), **comportement par défaut inchangé** et **câblage délibérément non fait** — `cli/src/lib/frontmatter.js:217-222` reflow à l'identique, câbler côté GUI seul aurait fabriqué une divergence GUI≠CLI. Gate Legolas **PASS** (345 tests CLI, 480 GUI, typecheck + lint clean, parité 8/8 et 17/17 intactes). *Écarts non bloquants du gate reportés en Ouverts ci-dessus.*
- [x] **Audit + amélioration d'Aragorn (v0.17.14)** — audit à 7 dimensions (Gandalf) puis exécution : QW-1/2 purge de la scorie Slack → canal iakaHub↔Discord ; QW-3/4 geste `iakaframe jalon` + obligation d'estimation dev au jalon P1→P2 dans la charte ; **CH-1** arbitrage décideur option A = `Task` accordé (delegation-guard actif) ; **CH-3** `Write` ciblé borné aux artefacts de pilotage (`specs/PROJET.md`), jamais de code ; **CH-2** sous-skill partagé `iakaframe-jalon` (Aragorn + Gandalf) ; **CH-4** garde anti-auto-cast **différée** par arbitrage (contractuel seul). Recompte 22→23 skills. Gate Legolas **FAIL puis PASS** : détection d'un **contrat fantôme** côté `iakaFrameGUI` (`Task` sans `Write`, mutuellement cohérent, sha-valide, 475 tests verts) → re-vendorage des 17 fixtures, commité en miroir (`iakaframe` v0.17.14 `3fc4c7d` / `iakaFrameGUI` v0.3.10 `8b3e63c`). *(instruction `specs/instructions/audit-amelioration-aragorn.md`.)*

- [x] **Anonymiser les URL Forgejo dans les kits sources (3/3 occurrences)** — `sjupin`→`<user>` dans `kits/iakaframe-openwebui/functions/iakaframe_identity_filter.py` (#3, commit `82c7fec`) puis, après **levée du régime §4 par arbitrage décideur**, dans `kits/iakaframe-claude/CLAUDE.md:60` et `kits/iakaframe-claude/global/CLAUDE.md:73` (#1/#2, commit `5acdbd0`). Host/IP `192.168.2.11:3001` conservé. Levée §4 consignée en note additive dans `reconcilier-kit-source-frame.md`. `grep sjupin kits/` = 0. Gates Legolas **PASS**. *(instruction `specs/instructions/anonymisation-url-forgejo-kits.md`.)*
- [x] **Réconcilier la forme de `services.json` (CLI ↔ ps1)** — direction retenue : aligner le retardataire sur la cible, `iakaframe-services.ps1` émet désormais l'enveloppe C-JSON `{ ok, generated, count, services }` (`[ordered]@{}`). Verrous Node V1 (forme fichier CLI `--out`) + V2 (garde statique du source ps1). Commit `515fe05`, gate Legolas **PASS**. *Runtime ps1 réel = gate humain différé (pas de `pwsh` sur la machine).* *(instruction `specs/instructions/reconciliation-services-json.md`.)*
- [x] **Anonymiser l'`author_url` Forgejo du kit OpenWebUI (occurrence #3)** — `kits/iakaframe-openwebui/functions/iakaframe_identity_filter.py:4` : `sjupin`→`<user>` (docstring non-exécuté, `py_compile` OK, `test_identity_filter.py` 15/15). Host/IP conservé. Commit `82c7fec`, gate Legolas **PASS**. Les occurrences #1/#2 restent ouvertes (voir §4 ci-dessus). *(instruction `specs/instructions/anonymisation-url-forgejo-kits.md`.)*
- [x] **Travailler la structure API & commandes du CLI** — surface `--json` harmonisée autour de la convention **C-JSON** (racine objet, `ok` en tête, collections pluriel + `count`, erreurs `{ok:false,error}` sur stdout + exit 1). Source unique `cli/src/lib/output.js`, extraction inline `portfolio`/`list` vers `lib/`, garde anti-dérive. 3 ruptures assumées (`list`, `assemble`, `services`). Commits `1356c2e`/`a8ec920`/`938ff91`, gate Legolas **PASS**. *(instruction `specs/instructions/cli-api-surface-harmonisation.md`.)*
- [x] **Nettoyer un chemin machine en dur** — `perimeter-guard.mjs` L186 + `README.md` L4 généralisés en formes génériques (`$HOME/...`, `/Users/<user>/...`, `C:\Users\<user>\...`), iso-comportement. Commit `01fa061`, gate Legolas **PASS**. *(instruction `specs/instructions/nettoyage-chemin-machine-perimeter-guard.md`.)*
